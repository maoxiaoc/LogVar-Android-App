# Logvar Android App 测试版 6.0.0 交接文档

更新日期：2026-09-21。

本次基线为 `v6.0.0-beta`，版本 `6.0.0-beta`、versionCode `9`、应用 ID `app.logvar`。历史背景见 [5.0.0 交接文档](交接文档-测试版5.0.0.md)，本次差异以 [更新记录](CHANGELOG.md) 为准。

## 发布与兼容性

- 继续使用现有签名，禁止替换签名或公开本地签名配置、密钥、Token、Cookie。
- 本次最后发布 arm64-v8a、armeabi-v7a、x86_64 三份 APK，之后只交付 arm64-v8a。
- APK 仅上传 GitHub Release，不加入源码提交；依赖目录、缓存、构建产物和本地私密文件继续排除。
- 三种架构构建及签名检查通过；arm64-v8a 已覆盖安装到测试手机，其他架构未进行真机验证。

## 缓存实现与维护

- Android 使用 `/api/cache/stats` 读取 ResourceCache 的 bytes 估算值，不能将搜索/弹幕内存数据与系统磁盘缓存混为一谈。
- 匹配数据读取 `.cache/bangumi-data-cache.json`；应用临时缓存读取 cacheDir 和 codeCacheDir。
- 保留进入首页、首页恢复前台和清理完成后的刷新方式，不添加每两秒缓存轮询。
- 搜索/弹幕清理必须保留 ResourceCache 对象；匹配数据清理不得被已开始的旧下载回写覆盖。
- Android 来源检测使用 AsyncLocalStorage 请求级配置与检测来源隔离的搜索缓存；不重启已运行服务，不全局修改来源策略。此检测入口属于 APK 内嵌服务的 Android 扩展。
- `server/danmu_api` 与 APK 内嵌目录并非完全相同：Android ResourceCache 和检测请求上下文在内嵌目录中。不要直接覆盖整个目录；共用接口修复应核对双方，Android 专用修改应保留。

## 验证

```powershell
node tests/mobile-resources.mjs
gradle :app:assembleRelease --offline
```

使用已配置的本地 JDK、Gradle、SDK/NDK 和签名，不重新下载工具链。APK 中实际运行的是 `app/src/main/assets/nodejs-project/`，源码更新后必须确认产物包含对应修改。

工作区操作限定在用户指定的 `E:\AI\Codex\LogVar\LogVar-Android-App`，不清理父级目录或其他项目。
