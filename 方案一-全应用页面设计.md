# LogVar Android App：方案一全应用页面设计

状态：设计基线。本文覆盖方案一的完整 App 页面与交互，不包含 Android 实现代码。

## 1. 全局结构

底部导航固定为三个主入口：**首页、来源、工具**。设置从首页右上角进入，避免底部导航拥挤。

```text
首页 ────────────────┐
来源 ── 来源检测详情 ├─ 设置
工具 ── 缓存确认 ────┘
```

视觉基调：深色、清晰、低干扰。绿色表示正常运行，橙色表示需要处理，红色只用于停止服务、检测失败和不可恢复的清理确认。

---

## 2. 首页：服务控制与 API 地址

首页回答三个问题：服务是否运行、电视该填什么地址、手机是否处于可用局域网。

```text
┌──────────────────────────────────┐
│ LogVar                       ⚙   │
│ 手机弹幕服务器                     │
├──────────────────────────────────┤
│ ● 服务运行中                       │
│ 正在监听局域网请求                  │
│                                  │
│ API 地址                          │
│ http://192.168.1.105:9321/•••••• │
│ [ 复制 API 地址 ]                  │
│                                  │
│ [ 启动服务 ]        [ 停止服务 ]   │
├──────────────────────────────────┤
│ 局域网状态                         │
│ ● 已连接：家庭 Wi‑Fi               │
│ ● 手机 IP：192.168.1.105           │
│ ● 端口 9321 正在监听               │
├──────────────────────────────────┤
│ 当前配置                           │
│ 已启用 7 个来源 · 已开启多源合并   │
│ 缓存占用 126 MB             [查看] │
├──────────────────────────────────┤
│   首页        来源        工具      │
└──────────────────────────────────┘
```

### 首页状态

| 状态 | 顶部文案 | 主要操作 |
|---|---|---|
| 服务运行中 | `● 服务运行中` | 复制 API 地址、停止服务 |
| 服务已停止 | `○ 服务已停止` | 启动服务 |
| 未连接 Wi‑Fi | `! 未连接 Wi‑Fi` | 显示说明，启动后等待网络恢复 |
| 启动失败 | `! 服务启动失败` | 查看错误、再次启动 |

- API 地址默认隐藏 Token 的中段；复制时复制完整地址。
- 点击“复制 API 地址”后显示“已复制，可填入播放器的自定义弹幕服务器”。
- 点击“停止服务”必须弹出确认框。
- “当前配置”区域点击后进入来源页；“查看”进入工具页。

---

## 3. 来源：启用、优先级、合并与检测

来源页是方案一已确认的核心页面。来源启用和搜索优先级使用同一个可拖动列表。

```text
┌──────────────────────────────────┐
│ 弹幕来源                           │
│ 拖动调整优先级，勾选后启用          │
├──────────────────────────────────┤
│ 搜索与下载来源                    │
│                                  │
│ ⠿  ☑ 腾讯视频                      │
│ ⠿  ☑ 爱奇艺                        │
│ ⠿  ☑ B站                           │
│ ⠿  ☑ 弹弹play                      │
│ ⠿  ☑ 芒果 TV                       │
│ ⠿  ☑ 优酷                          │
│ ⠿  ☑ 人人视频                      │
│                                  │
│ 已启用 7 个来源                    │
├──────────────────────────────────┤
│ 多源弹幕合并                 [开] │
│ 将当前已启用来源的同剧同集弹幕合并  │
│ 不同版本、片头或剪辑可能产生偏移。  │
├──────────────────────────────────┤
│ 来源检测                           │
│ 检测会执行：搜索 → 分集 → 下载弹幕  │
│                                  │
│ [ 检测已启用来源 ]                 │
│                                  │
│ ● 腾讯视频   正常 · 201 条         │
│ ● 爱奇艺     正常 · 3,779 条       │
│ ● B站        正常 · 3,901 条       │
│ ● 弹弹play   正常 · 2,569 条       │
│ ● 芒果 TV    正常 · 6,777 条       │
│ ● 优酷       正常 · 6,516 条       │
│ ● 人人视频   正常 · 3,983 条       │
│                                  │
│ 上次检测：今天 20:30               │
├──────────────────────────────────┤
│       有未应用的设置                │
│      [ 应用并重启服务 ]             │
├──────────────────────────────────┤
│   首页        来源        工具      │
└──────────────────────────────────┘
```

### 来源页规则

- 从上到下就是搜索、匹配与下载优先级。
- 未勾选来源变灰、保留位置，不参与搜索、下载、合并或检测。
- 合并只使用当前已勾选来源。
- 检测按当前启用来源逐个执行，避免并发请求触发平台频率限制。
- 修改勾选、排序或合并开关后，仅显示“有未应用的设置”；用户点击“应用并重启服务”后才生效。
- 检测失败行显示红点；点击后进入“来源检测详情”。

---

## 4. 来源检测详情

用于解释某个来源为什么失败，避免只显示模糊的“不可用”。

```text
┌──────────────────────────────────┐
│ ‹  B站检测结果                     │
├──────────────────────────────────┤
│ ● 检测正常                         │
│ 完成于今天 20:30 · 用时 8 秒       │
├──────────────────────────────────┤
│ 1. 搜索测试内容              正常  │
│ 2. 读取分集                  正常  │
│ 3. 下载第 1 集弹幕      3,901 条  │
├──────────────────────────────────┤
│ [ 重新检测 B站 ]                   │
└──────────────────────────────────┘
```

失败时将第三段替换为失败阶段、简短错误原因与“重新检测”按钮；错误详情中自动隐藏 Token。

---

## 5. 工具：缓存与局域网排查

工具页聚合可清理数据和电视无法连接时需要的诊断信息。

```text
┌──────────────────────────────────┐
│ 工具                               │
├──────────────────────────────────┤
│ 缓存管理                           │
│                                  │
│ 搜索缓存                  12 MB    │
│ [ 清理搜索缓存 ]                   │
│                                  │
│ 弹幕缓存                 104 MB    │
│ [ 清理弹幕缓存 ]                   │
│                                  │
│ 匹配数据缓存              10 MB    │
│ [ 清理匹配数据缓存 ]               │
│                                  │
│ 合计                      126 MB   │
│ [ 清理全部缓存 ]                   │
├──────────────────────────────────┤
│ 局域网状态                         │
│ ● Wi‑Fi：家庭 Wi‑Fi                │
│ ● 手机 IP：192.168.1.105            │
│ ● API 端口：9321，正在监听          │
│ ● 服务：运行中                      │
├──────────────────────────────────┤
│   首页        来源        工具      │
└──────────────────────────────────┘
```

### 缓存规则

- 清理缓存不会删除 Token、来源排序、来源开关或合并设置。
- “清理全部缓存”需要二次确认，并明确列出会删除的三种缓存。
- 缓存清理完成后刷新大小；用户需要时可在播放器重新搜索。

---

## 6. 设置：Token 与应用行为

从首页右上角齿轮进入。此页不放在底部导航。

```text
┌──────────────────────────────────┐
│ ‹  设置                            │
├──────────────────────────────────┤
│ API Token                          │
│ ••••••••••••••••••••••••  [复制]  │
│ [ 显示完整 Token ]                 │
│ [ 重新生成 Token ]                 │
│                                  │
│ 管理 Token                          │
│ ••••••••••••••••••••••••  [复制]  │
│ 仅用于管理页面，不填入播放器。      │
├──────────────────────────────────┤
│ 服务行为                           │
│ 保持前台运行                 [开] │
│ 开机后自动启动               [关] │
├──────────────────────────────────┤
│ 关于                               │
│ LogVar Android App                 │
│ 服务版本与修复状态            [查看]│
└──────────────────────────────────┘
```

- 重置普通 API Token 前，明确提示旧电视地址会立即失效。
- 管理 Token 不在首页或来源页出现。
- “开机后自动启动”默认关闭，必须由用户手动开启。

---

## 7. 确认框与反馈

### 停止服务

```text
停止弹幕服务器？
电视和其他局域网设备将暂时无法获取弹幕。

[取消]  [停止服务]
```

### 应用来源设置

```text
应用来源设置并重启服务？
正在播放的客户端需重新搜索或重新加载弹幕。

[继续编辑]  [应用并重启]
```

### 清理全部缓存

```text
清理全部缓存？
将删除搜索、弹幕和匹配数据缓存；不会删除 Token 或来源设置。

[取消]  [清理]
```

所有复制、启动、停止、检测完成和缓存清理操作，使用页面底部短提示反馈结果，不遮挡当前操作。

---

## 8. 第一版页面范围

| 页面 | 第一版 |
|---|---|
| 首页与 API 地址 | 是 |
| 来源、排序、合并与检测 | 是 |
| 来源检测详情 | 是 |
| 工具、缓存与局域网状态 | 是 |
| 设置与 Token 管理 | 是 |
| 运行日志全文浏览 | 后续版本 |
| 配置导入与导出 | 后续版本 |

## 9. 工作区边界

Android App 的所有代码、构建产物、测试数据、设计文件和临时文件只能写入：

```text
E:\AI\Codex\LogVar\LogVar-Android-App
```

现有 `E:\AI\Codex\LogVar` 仅作为可读取的服务源码、修复逻辑、部署文档和验证脚本来源；需要使用的资源须复制到 Android App 专用工作区后再改造。
