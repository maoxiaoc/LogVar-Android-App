# Logvar Android App 测试版 3.0.0 交接文档

更新日期：2026-09-20

## 1. 交接目标

以当前测试版 3.0.0 为稳定基线，在新的独立工作区继续增加功能、修复弹幕源和优化 Android 界面。新开发应保持现有应用 ID、签名和用户配置兼容，除非新需求明确要求变更。

当前工程目录：

```text
E:\AI\Codex\LogVar\LogVar-Android-App
```

GitHub 仓库：<https://github.com/maoxiaoc/LogVar-Android-App>

当前发布页：<https://github.com/maoxiaoc/LogVar-Android-App/releases/tag/v3.0.0-beta>

## 2. 当前版本基线

| 项目 | 当前值 |
| --- | --- |
| 应用名称 | Logvar |
| applicationId | `app.logvar` |
| versionName | `3.0.0-beta` |
| versionCode | `2` |
| minSdk | 26（Android 8.0） |
| targetSdk | 35 |
| compileSdk | 36 |
| NDK | 27.3.13750724 |
| Java | 17 |
| Android Gradle Plugin | 8.7.3 |
| Kotlin | 2.1.20 |
| Compose Material 3 | 1.4.0 |

GitHub 的 `v3.0.0-beta` 标签对应已验证的应用源码基线。主分支在此基础上还包含后续 README 文档更新。

## 3. 已实现功能

- 在 Android 本机运行弹幕 HTTP API，无需 Termux 或外部服务器。
- 启动、停止和监控独立的 `:server` 前台服务进程。
- 根据 Wi-Fi 或手机热点网络展示可供播放器访问的局域网地址。
- API Token 和管理 Token 分离，支持复制、遮蔽显示和重新生成。
- 管理腾讯视频、爱奇艺、B站、弹弹play、芒果 TV、优酷和人人视频弹幕源。
- 支持启停来源、拖拽调整优先级、多源合并和配置持久化。
- 来源检测进入独立结果界面，顺序执行搜索、分集读取和单集弹幕下载检测。
- 支持搜索、弹幕和匹配数据缓存统计及清理。
- 支持保持前台运行和开机启动设置。
- 支持顶部返回按钮、系统返回键和系统侧滑返回：
  - 设置 → 首页
  - 服务信息 → 设置
  - 来源检测结果 → 弹幕源
  - 其他非首页页面 → 首页
- 服务信息页目前只显示内置服务版本。

## 4. 代码结构与职责

```text
app/src/main/java/app/logvar/
  MainActivity.kt           Compose 界面、页面状态、设置和 ServiceStore
  ServerService.java        独立 :server 前台服务及 Node 生命周期
  NodeRuntime.java          Java 与原生 Node 运行时桥接
  ServiceLifecycle.kt       服务运行状态与生命周期辅助
  BootReceiver.java         开机启动入口
  ShareNetwork.kt           当前网络与共享地址读取
  ShareAddressPolicy.java   Wi-Fi/热点地址选择规则
  SourceOrderList.kt        弹幕源可拖拽列表
  SourceDrag.java           拖拽排序计算

app/src/main/cpp/
  logvar_native.cpp         Node 原生运行入口

app/libnode/
  bin/                      三种 ABI 的 libnode.so
  include/                  Node 原生头文件

app/src/main/assets/nodejs-project/
  main.cjs / main.js        APK 内置服务启动入口
  danmu_api/                APK 实际运行的服务端代码
  node_modules/             APK 打包使用的运行依赖

server/
  danmu_api/                可维护的服务端源码副本
  package.json              服务端依赖清单
  package-lock.json         锁定依赖版本

scripts/
  prepare-android-assets.ps1 重新安装并同步 APK 内置 node_modules

tests/
  mobile-resources.mjs      缓存和并发资源控制检查
  ShareAddressCheck.java    网络共享地址规则检查
  SourceDragCheck.java      拖拽排序逻辑检查

releases/
  2.0.0-beta/              旧测试版归档
  3.0.0-beta/              当前三个 ABI 的已签名 APK 和发行说明
```

## 5. 服务端修改的同步规则

APK 运行的是：

```text
app/src/main/assets/nodejs-project/danmu_api/
```

`server/danmu_api/` 是维护和依赖管理使用的服务端源码副本。修改弹幕源、API、缓存或并发逻辑时，必须保证对应改动同步到 APK 内嵌目录，否则本地源码看似已修复，生成的 APK 仍会运行旧代码。

`scripts/prepare-android-assets.ps1` 当前只根据 `server/package-lock.json` 安装依赖并更新内嵌 `node_modules`，不会自动复制 `server/danmu_api`。完成服务端修改后，应在构建前单独核对两个 `danmu_api` 目录的差异。

完成标准：源码副本与 APK 内嵌副本包含同一项功能修复，且相关 JavaScript 文件通过 `node --check` 或现有测试。

## 6. 新工作区建立步骤

### 第一步：建立独立 Git 工作区

从 GitHub 克隆仓库到新的空目录，使 `LogVar-Android-App` 自身成为 Git 仓库根目录。当前本地目录实际处于父级 `E:\AI\Codex\LogVar` Git 仓库下，并未形成独立的本地仓库，不适合作为下一阶段长期开发环境。

完成标准：在新目录执行 `git rev-parse --show-toplevel` 时，返回新的 `LogVar-Android-App` 目录；`git status` 能正确显示应用工程内部修改。

### 第二步：迁移本地私密配置

以下文件不在 GitHub 中，需要从当前工作区安全复制到新工作区：

- `local.properties`
- `release-signing.properties`
- `release-signing.properties` 中 `storeFile` 指向的签名密钥文件

签名密码和密钥继续保留在本地，不提交到 GitHub，不写入交接文档或聊天记录。

完成标准：新工作区能够读取 Android SDK、NDK 和 release 签名，且 `git status` 不会把这些文件列为待提交内容。

### 第三步：迁移本地构建资源

根据新工作区位置更新 `local.properties`。当前 NDK 位于：

```text
E:\AI\Codex\LogVar\LogVar-Android-App\.toolchain\ndk\android-ndk-r27d
```

可以继续引用旧路径，也可以将 NDK 迁移到 Android SDK 的标准 `ndk\27.3.13750724` 目录后更新配置。

以下内容可以按需迁移，不属于源码：

- `.gradle-user/`：约 1.25 GB，可减少首次离线构建下载。
- `.toolchain/ndk/`：当前原生构建所需。
- `releases/`：2.0.0 和 3.0.0 的本地发行归档。

完成标准：Gradle 能找到 JDK、SDK、NDK 和 CMake；旧版发行归档仍有独立备份。

### 第四步：建立开发基线

在新增功能前先执行一次不修改功能的基线构建和测试：

```powershell
node tests/mobile-resources.mjs
gradle :app:assembleRelease --offline
```

如新工作区没有依赖缓存，去掉 `--offline`，并在需要更新内嵌 Node 依赖时执行：

```powershell
.\scripts\prepare-android-assets.ps1
```

完成标准：测试通过，三个 ABI 的 release APK 成功生成，签名证书与测试版 3.0.0 一致。

## 7. 签名与版本规则

- 后续版本继续使用当前签名，才能覆盖安装并保留用户数据。
- 发布前检查实际签名证书 SHA-256：

```text
7f2bf47fe3e4f09cd53d3489c774d15d2481fcf470001e167c89bd364bbf44e6
```

- 修改 `versionName` 时同步更新发布说明和 APK 文件名。
- 正式产生比 3.0.0 更新的安装包时，应提高 `versionCode`。继续使用相同或更低的 `versionCode` 会影响常规覆盖安装和回退行为。
- Release 构建启用了 R8、资源压缩和按 ABI 拆包。
- 不要把 `release-signing.properties`、密钥、Token、Cookie、`.env` 或本地路径配置上传到 GitHub。

## 8. 测试版 3.0.0 发行校验值

| ABI | 文件 | SHA-256 |
| --- | --- | --- |
| arm64-v8a | `Logvar-3.0.0-beta-arm64-v8a.apk` | `CBE2576FCD861D838C3A284A1010F24B7EE1729F546EB3967297BB59647BDF45` |
| armeabi-v7a | `Logvar-3.0.0-beta-armeabi-v7a.apk` | `2871CD032ECE6A9B85EF72F710B2D4EDA9F67F23B98D27931E889F1B5B44DD72` |
| x86_64 | `Logvar-3.0.0-beta-x86_64.apk` | `8946AE69F402718D231C99B1FFC9C30E58CF31EDFAAB9E8F9AC6B9954DD5B9EF` |

测试手机使用 `arm64-v8a` 包。三个 APK 都使用同一应用 ID和签名，每次只安装与设备架构匹配的一份。

## 9. 已验证状态

- `assembleRelease --offline` 已成功完成，包括 lint、R8 和资源压缩。
- `tests/mobile-resources.mjs` 已验证缓存预算、到期释放、清理、并发上限和结果顺序。
- 测试手机已覆盖安装 arm64-v8a 版本并正常启动。
- 服务健康接口曾返回 `ready=true`。
- 停止服务后独立 `:server` 进程退出；重新启动会创建新进程并恢复 HTTP 服务。
- 系统返回键、侧滑返回和页面左上角返回路径已在测试手机验证。

单次无播放器请求的内存快照：

- 前台 UI + 服务：约 166.2 MiB。
- 后台 UI + 服务：约 108.9 MiB。
- 停止服务后后台 UI：约 39.2 MiB。

这些数据是单次场景快照，不能代表长期播放、高并发或所有手机的稳定占用。

## 10. 后续开发注意事项

- 弹幕源依赖第三方平台接口，零条结果与接口失效需要分开判断；诊断时保留每一步的真实错误信息。
- 来源检测会临时执行搜索、读取分集和下载弹幕，完成后应恢复用户原有来源配置和服务运行状态。
- 拖拽排序同时涉及 Compose 手势、动画和最终持久化顺序，修改后至少验证快速拖动、跨多项拖动、取消拖动和重启后顺序。
- 手机热点地址与普通 Wi-Fi 地址走不同网络场景，修改网络逻辑后分别验证。
- Node 服务运行在独立进程，UI 进程和服务进程的内存、崩溃与生命周期应分别观察。
- 首页不拦截系统返回；其他页面继续遵守第 3 节中的返回映射。
- 保持现有 DataStore/SharedPreferences 配置键兼容，避免覆盖安装后丢失 Token、来源顺序或用户设置。
- 新功能完成后先生成测试 APK并安装验证，再替换 `releases` 归档和 GitHub Release。

## 11. 当前工作区清理状态

已删除 Android 构建产物、工程 Gradle 缓存、构建日志、旧截图、根目录重复 APK、工具链压缩包、重复 Node 工具链、npm 缓存和 `server/node_modules`，共释放约 4.05 GB。

仍保留源码、APK 内嵌依赖、`app/libnode`、当前 NDK、`.gradle-user`、签名配置以及 2.0.0/3.0.0 发行归档。清理后的工作区约 4.16 GB。

`releases/3.0.0-beta/RELEASE.md` 中“根目录仍保留 2.0.0 APK”的旧描述已因本次清理失效；根目录重复 APK 已删除，唯一保留副本位于 `releases/2.0.0-beta/`。

## 12. 新任务建议起始指令

在新的开发任务中，可以直接提供以下说明：

> 工作区是 Logvar Android App 的独立 Git 仓库。请先阅读 `交接文档-测试版3.0.0.md`、`README.md` 和 `releases/3.0.0-beta/RELEASE.md`。以测试版 3.0.0 为稳定基线继续开发，保持 `app.logvar`、现有签名和用户配置兼容。修改服务端代码时同步检查 `server/danmu_api` 与 `app/src/main/assets/nodejs-project/danmu_api`，完成后运行相关测试和 release 构建。未经明确要求，不替换 2.0.0 或 3.0.0 发行归档。
