# 更新记录

## 测试版 8.0.0（2026-09-22）

以下为相对测试版 6.0.0 的变更。7.0.0 为已弃用的本地对比方案，不作为公开版本发布。

### 布局与屏幕适配

- 统一 edge-to-edge 和系统安全区处理，Scaffold 内边距集中应用并消费，避免子页面重复计算顶部安全区造成过大留白。
- 根据实际可用窗口宽高选择紧凑布局，不按手机型号或物理分辨率硬编码；同一个 APK 兼容不同屏幕。
- 减少紧凑布局中首页服务卡、局域网卡片的多余高度和留白，调整设置页间距，改善按钮和缓存入口需要过多滚动才能看到的问题；长页面仍允许滚动。
- 紧凑布局按钮最小高度为 48dp、列表行为 64dp；来源拖拽坐标同步使用实际行高。
- 通用列表标题与说明支持换行，减少长文字被省略。遵循系统字体缩放，较大字体时不启用紧凑布局。

### 操作提示统一

- 启停服务、复制、应用策略、Token 更新、缓存清理及失败提示统一为底部居中的浅色悬浮胶囊，替代默认深色提示条。
- 所有提示统一采用 primaryContainer / onPrimaryContainer 动态配色，以图标和文字区分状态。短文案按内容收窄，长文案自动换行，失败提示可关闭。
- 提示放在独立根浮层，统一预留系统安全区和底部导航空间，修复切换到缓存管理等无底栏页面时提示向下跳动的问题。
- 保留框架提示生命周期、无障碍超时和淡入淡出，加入轻微上浮动画；启动完成文案统一为“服务已启动”。

### 发布与验证

- 仅发布 arm64-v8a APK，沿用原签名；版本名 8.0.0-beta，versionCode 从 9 升至 10，可覆盖升级兼容的 6.0.0 并保留数据。
- 保留 6.0.0 的缓存统计、分类清理、首页进入/恢复及清理后刷新、来源检测隔离、弹幕策略和本机/局域网地址逻辑。本次不新增这些功能，也不加入缓存定时轮询。
- 增加窗口布局策略、系统安全区与提示定位/配色的回归检查，并纳入 CI。源码检查不替代真机渲染测试。
- release 构建和原签名校验通过；已安装至一加 8T，用户确认满意。一加 13 的当前版本实际显示仍待验证，不宣称所有设备均已实测。

## 测试版 6.0.0（2026-09-21）

以下为相对测试版 5.0.0 的变更。

### 缓存统计与显示

- 修复首页及缓存管理页无法反映实际服务缓存占用的问题：通过新增的缓存统计接口读取搜索、弹幕缓存的内存数据估算值，不再用无关磁盘文件代替这些缓存。
- 匹配数据缓存按实际文件大小读取；新增“应用临时缓存”统计与单独清理，覆盖应用 cacheDir 和 codeCacheDir。
- 修复读取失败被当作零缓存的问题，无法取得服务统计时显示“暂时无法读取”，不伪装成 0 KB。
- 修复小于 1 KB 的非空缓存被整数截断成 0 KB：小缓存显示 B，较大缓存显示带小数的 KB/MB。
- 进入首页时刷新；停留在首页从后台恢复前台时也刷新。清理按钮等待后端完成后立即重读，并同步首页和缓存管理页。不对缓存进行每两秒定时轮询。

### 缓存清理

- 修复搜索、弹幕缓存清理时用普通 Map 替换缓存对象的问题；现在原地清空，保留容量限制、过期回收和占用统计能力。
- 匹配数据清理不再主动触发立即重新下载；清理期间已经开始的旧下载不能再回写缓存。后续业务请求需要该数据时仍可重新生成缓存。
- 文件删除失败不再被吞掉后显示成功；清理后的统计读取失败会另行提示。
- Android“全部清理”明确针对展示的缓存类别，不再顺带重置其他服务状态；Token 和来源配置不受影响。

### 来源检测与界面

- 服务已运行时，来源检测不再为每个来源停止、重启服务，避免因此清空原有内存缓存和中断服务。
- 每次检测使用独立请求配置，按指定来源检测；搜索缓存按检测来源隔离，避免不同来源相互误用搜索结果，不改用户保存的来源策略。
- 弹幕策略选择“仅保留弹幕最多的来源”后，“当前选择”说明缩短为“只显示弹幕最多的来源”。

### 安装与验证说明

- 应用版本为 `6.0.0-beta`，versionCode 从 8 升至 9；保持 `app.logvar` 和原 release 签名，支持覆盖升级。
- 本次发布 arm64-v8a、armeabi-v7a、x86_64 三份 APK，是最后一次发布三个架构；后续只发布 arm64-v8a。
- 三份 APK 均完成 release 构建和签名/架构校验。缓存统计、清理、到期回收、容量限制、并发上限及来源请求隔离回归测试通过。
- arm64-v8a 已在 OnePlus 8T 覆盖安装，用户已确认缓存修复完成；另外两个架构未做真机运行验证。
- 搜索/弹幕数值是内存数据估算，匹配数据/临时缓存是磁盘大小；这里的合计不等于 Android 系统设置中的“存储”或“缓存”。重启服务会释放内存缓存，未产生可缓存结果或缓存过期后显示零属于正常情况。

本版本仍为测试版，第三方弹幕源可用性与设备兼容性需要继续验证。
